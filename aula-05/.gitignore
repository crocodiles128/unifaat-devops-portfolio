# Provider plugins e state local (nunca comitar)
.terraform/
*.tfstate
*.tfstate.backup
tfplan

# Variáveis com segredos (senha do RDS)
terraform.tfvars
*.auto.tfvars
!terraform.tfvars.example

# Chaves SSH
*.pem
.ssh/
